# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True

# Database
DB_NAME=trading_signals
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# Encryption key for broker API keys (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-fernet-key-here
